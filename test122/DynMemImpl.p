from pollen.utils import DynamicMemoryProtocol
!--
An implementation of DynamicMemoryProtocol 
Not fully implemented.
--!

module DynMemImpl implements DynamicMemoryProtocol {
    public free(uint8 bytes) {
    }
    public malloc(uint8 bytes) {
    }
}
