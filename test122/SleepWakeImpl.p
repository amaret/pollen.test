from pollen.hardware import SleepWakeProtocol
!--
An implementation of SleepWakeProtocol 
--!

module SleepWakeImpl implements SleepWakeProtocol {

    +{ #include <stdio.h> }+

    public sleep(uint8 mode) {
    }
    public wake() {
    }
}
